generated: '2026-09-03'
method: derived
source: mcp/leadping-mcp.yml (probed tool lists + provider MCP documentation) bound to openapi/leadping-openapi.json
  operationIds
surfaces:
  openapi:
    file: openapi/_original/leadping-openapi.json
    url: https://api.leadping.ai/openapi.json
    operations: 144
    gated: false
    note: 'Re-harvested 2026-09-03: 144 operations across 119 paths, up from 137/113 on 2026-08-18. New
      since then: A2A_GetAgentCard, A2A_HandleRequest, Leads_Assign, Conversations_MarkAsRead, Organizations_IssueApiKey
      / GetApiKey / DeleteApiKey, Organizations_LeaveCurrentOrganization, Sources_RotateApiKey, Users_UpdateNotificationPreferencesForCurrentUser
      and two BlogArticles operations. Removed: Contact_Submit, Contact_SubmitWebsite, Leads_CreateIntakeFromQuery,
      Organizations_AcceptInvitation, Organizations_GetInvitationByToken.'
    refined_specs_note: 'The per-tag refined specs in openapi/*.yml were split from the 137-operation
      contract harvested 2026-08-18 and were last rebuilt 2026-08-27. openapi/_original/leadping-openapi.json
      now holds the 144-operation contract re-harvested 2026-09-03, so the refined set is one contract
      behind: it is missing the twelve added operations and still carries the five removed ones. A refine-openapis
      run is required to reconcile them. Recorded rather than silently resolved because splitting specs
      is a separate pipeline, not this one.'
  mcp:
  - url: https://leadping.ai/docs/mcp
    gated: false
    note: Anonymous tools/list returned 3 tools with real inputSchemas on 2026-08-18.
  - url: https://api.leadping.ai/mcp
    gated: true
    note: 401 without a bearer credential. Capability groups below are mapped from the provider's published
      MCP documentation by name and semantics, not from an authenticated tools/list.
  graphql: null
  a2a:
    url: https://api.leadping.ai/a2a
    card: a2a/leadping-agent-card.json
    gated: true
    protocol_binding: JSONRPC
    skills: 1
    note: 'NEW since 2026-08-18. One read-only skill, lead-research, which the card describes as returning
      an organization-scoped lead snapshot with conversations and recent activity. It requires a bearer
      token, so its live request/response shape could not be introspected anonymously; the binding below
      is by name and semantics from the published card, not from an authenticated call. The A2A surface
      is also reachable through REST: A2A_GetAgentCard and A2A_HandleRequest are declared operations in
      the OpenAPI.'
  webmcp:
    url: https://leadping.ai/.well-known/mcp/server-card.json
    gated: false
crosswalk:
- tool: 'org-tools: read current organization'
  category: organizations
  rest:
  - Organizations_GetForCurrentUser
  binding: semantic
  confidence: high
  note: 'Docs: "Reading the current organization". GET /organizations/me is the only such operation.'
- tool: 'org-tools: list organization-switch options'
  category: organizations
  rest:
  - Organizations_GetSwitchOptions
  binding: semantic
  confidence: high
- tool: 'org-tools: list leads'
  category: leads
  rest:
  - Leads_GetAllForCurrentUser
  binding: semantic
  confidence: high
- tool: 'org-tools: read a lead'
  category: leads
  rest:
  - Leads_GetForCurrentUser
  binding: semantic
  confidence: high
- tool: 'org-tools: create a lead'
  category: leads
  rest:
  - Leads_CreateExternal
  - Leads_CreateIntake
  binding: semantic
  confidence: medium
  note: Docs say "Creating a lead can start configured routing and automation workflows". Three REST intake
    operations exist (POST /leads, POST /leads/intake, GET /leads/intake); which one the tool wraps is
    not stated. GET /leads/intake (Leads_CreateIntakeFromQuery) is a query-string variant, unlikely.
- tool: 'org-tools: update a lead'
  category: leads
  rest:
  - Leads_Update
  binding: semantic
  confidence: high
- tool: 'org-tools: archive a lead'
  category: leads
  rest:
  - Leads_Archive
  binding: semantic
  confidence: high
- tool: 'org-tools: restore a lead'
  category: leads
  rest:
  - Leads_Unarchive
  binding: semantic
  confidence: high
- tool: 'org-tools: add lead tags'
  category: leads
  rest:
  - Leads_AddTags
  binding: semantic
  confidence: high
- tool: 'org-tools: replace lead tags'
  category: leads
  rest:
  - Leads_ReplaceTags
  binding: semantic
  confidence: high
- tool: 'org-tools: remove a lead tag'
  category: leads
  rest:
  - Leads_RemoveTag
  binding: semantic
  confidence: high
- tool: 'org-tools: permanently delete a lead'
  category: leads
  rest:
  - Leads_Delete
  binding: semantic
  confidence: high
  note: Docs flag this as consequential and requiring explicit confirmation.
- tool: 'org-tools: list conversations'
  category: conversations
  rest:
  - Conversations_GetAllForCurrentUser
  binding: semantic
  confidence: high
- tool: 'org-tools: list conversations for a lead'
  category: conversations
  rest:
  - Conversations_GetConversationsByLead
  binding: semantic
  confidence: high
- tool: 'org-tools: list events'
  category: events
  rest:
  - Events_GetAllForCurrentUser
  - Events_GetAllForOrganization
  binding: semantic
  confidence: medium
- tool: 'org-tools: list lead events'
  category: events
  rest:
  - Events_GetAllForLead
  binding: semantic
  confidence: high
- tool: 'org-tools: read event details'
  category: events
  rest:
  - Events_GetById
  - Events_GetDetailById
  binding: semantic
  confidence: medium
- tool: 'org-tools: list sources'
  category: sources
  rest:
  - Sources_GetAllForCurrentUser
  binding: semantic
  confidence: high
- tool: 'org-tools: read a source'
  category: sources
  rest:
  - Sources_Get
  binding: semantic
  confidence: high
- tool: 'org-tools: read source metrics'
  category: sources
  rest:
  - Sources_GetMetrics
  binding: semantic
  confidence: high
- tool: 'a2a: lead-research'
  surface: a2a
  category: leads
  rest:
  - Leads_GetForCurrentUser
  - Conversations_GetConversationsByLead
  - Events_GetAllForLead
  binding: semantic
  confidence: medium
  note: Mapped from the A2A card skill description - "an organization-scoped lead snapshot with conversations
    and recent activity" - to the three REST reads that compose that snapshot. The A2A endpoint is auth-gated
    so the skill's real input/output schema could not be introspected; confidence is medium because the
    composition is inferred from the description, not observed. The card declares outputMode application/vnd.leadping.lead-research+json,
    a media type that appears nowhere in the OpenAPI, so the A2A response shape is not documented in the
    REST contract.
mcp_only:
- tool: search_leadping_docs
  server: https://leadping.ai/docs/mcp
  reason: Documentation retrieval over the Mintlify docs index. No REST equivalent.
- tool: query_docs_filesystem_leadping_docs
  server: https://leadping.ai/docs/mcp
  reason: Virtual docs filesystem query. No REST equivalent.
- tool: submit_feedback
  server: https://leadping.ai/docs/mcp
  reason: Docs-team feedback channel. Distinct from the REST Feedback_Create operation, which files product
    feedback against an organization.
- tool: leadping.get_public_resources
  server: webmcp
  reason: Browser-side resource lookup. No REST equivalent.
- tool: leadping.get_authentication_guidance
  server: webmcp
  reason: Browser-side static guidance. No REST equivalent.
- tool: leadping.open_public_page
  server: webmcp
  reason: Browser navigation action. No REST equivalent.
- tool: leadping.open_agent_resource
  server: webmcp
  reason: Browser navigation / resource fetch action. No REST equivalent.
rest_only:
  summary: 'The organization-tools MCP server covers leads, conversations, events, sources and organization
    context. Everything else in the 137-operation REST contract has no documented tool: the whole communications
    surface (Sms, Calls, CallEvents, SmsEvents, Telephony, PhoneNumbers, OutboundDelivery), compliance
    (Suppressions), workflow (Automations, LeadStatuses, LeadStatusChanges, Tags CRUD), billing (PaymentMethods,
    Transactions, Usage, Wallets), plus Analytics, Notifications, Users, UserDataExports, Feedback and
    Contact.'
  tags_with_no_tool:
  - Analytics
  - Automations
  - CallEvents
  - Calls
  - Contact
  - Feedback
  - LeadStatusChanges
  - LeadStatuses
  - Notifications
  - OutboundDelivery
  - PaymentMethods
  - PhoneNumbers
  - Sms
  - SmsEvents
  - Suppressions
  - Tags
  - Telephony
  - Transactions
  - Usage
  - UserDataExports
  - Users
  - Wallets
coverage:
  rest_operations: 144
  mcp_tools_mapped: 20
  mcp_only_tools: 7
  rest_tags_total: 27
  rest_tags_covered_by_mcp: 5
  note: Tool counts for the organization-tools server are capability groups read from documentation, not
    a live tool count. An authenticated tools/list is required to make these exact. The single A2A skill
    is mapped by semantics from the published agent card; the A2A endpoint returned 405 to an anonymous
    GET and requires a bearer token for a real JSONRPC call.
  a2a_skills_mapped: 1
